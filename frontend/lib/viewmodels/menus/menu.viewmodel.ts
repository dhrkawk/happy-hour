export interface MenuFormViewModel {
  name: string;
  price: number;
  category: string;
}

export interface MenuListItemViewModel {
  id: string;
  name: string;
  price: number;
  thumbnailUrl: string;
  category: string;
}
